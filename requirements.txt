# Core runtime dependencies for the glare package.
# Mirrors [project.dependencies] in pyproject.toml.
#
# CuPy is an optional, CUDA-specific dependency (no universal wheel). Install the
# wheel matching your CUDA toolkit, e.g.:  pip install cupy-cuda12x
# or use the packaging extra:  pip install glare[cuda12]   (cuda11 / cuda12 / cuda13)
numpy>=1.21.0
scipy>=1.7.0
xarray>=2022.3.0
netcdf4>=1.5.0
pyproj>=3.3.0
pytz>=2021.3
pysolar>=0.10
